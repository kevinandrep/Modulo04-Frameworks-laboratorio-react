import { AppLayout } from "../layouts/app.layout";
import { ListContainer } from "../pods/list-git/list-git.container";

export const ListGitScene = () => {
  return (
    <AppLayout>
      <ListContainer></ListContainer>
    </AppLayout>
  );
};
